{
  "name": "detoxify",
  "version": "2.0.0",
  "private": true,
  "description": "Browser extension to detox your YouTube experience — hide Shorts, Playables, and more.",
  "type": "module",
  "scripts": {
    "build": "bun scripts/build.mjs",
    "build:watch": "bun scripts/build.mjs --watch"
  },
  "packageManager": "bun@1.3.10",
  "devDependencies": {
    "esbuild": "^0.25.0"
  },
  "engines": {
    "node": ">=18"
  }
}
